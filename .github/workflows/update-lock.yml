name: Update Cargo Lock

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-lock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          token: ${{ secrets.GITHUB_TOKEN }}
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - name: Update Cargo.lock (cacao only)
        run: cargo update -p cacao -p cidre -p cidre-macros
      - name: Commit and push
        run: |
          git config user.name "jiyuan1020"
          git config user.email "jiyuan1020@users.noreply.github.com"
          git add Cargo.lock
          if git diff --cached --quiet; then
            echo "NO_CHANGES"
          else
            git commit -m "chore: update Cargo.lock for cacao branch HEAD"
            git push origin master
          fi